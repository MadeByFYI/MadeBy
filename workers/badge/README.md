# workers/badge (stub)

Cloudflare Worker that **live-serves** the badge SVG (`madeby.fyi/b/<owner>/<repo>.svg`) and is
always wrapped in a link to the authoritative resolver page. The badge is a *pointer, never
standalone proof*.

Filled by the badge ticket under the producer-push epic (#6). Empty until then.
