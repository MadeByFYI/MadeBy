{
  "name": "madeby",
  "private": true,
  "version": "0.0.0",
  "description": "Who made this thing? — verifiable content provenance, starting with code.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm -r --if-present build",
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test",
    "provenance:verify-trailers": "node scripts/verify-trailers.mjs",
    "provenance:capture": "node scripts/capture-spans.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
