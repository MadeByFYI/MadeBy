# services/ingestion (stub)

Python service on **Modal** (cold path): bounded public-git ingestion (GH Archive / BigQuery +
GitHub App), multi-resolution fingerprinting, and human/AI classification. Cost-bounded —
top-N + on-demand, not all of git (`OPERATIONS.md` §3).

Not a pnpm workspace member (separate Python toolchain). Filled by **#10** (and #11/#12). Empty until then.
