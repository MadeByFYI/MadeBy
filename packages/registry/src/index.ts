// @madeby/registry — subject lookup behind the asker-pull resolver. Interface + in-memory v0;
// resolution policy comes from @madeby/core.

export { InMemoryRegistry, resolveByFingerprint, resolutionContext, demoRegistry } from "./registry";
export type { Registry } from "./registry";
