// @madeby/badge — the live-served badge SVG. Dependency-free; reusable by the Next route now
// and the Cloudflare Worker later. The badge is a headline + pointer, never standalone proof.

export { renderBadge, mixMessage, tierColor, TIER_COLORS } from "./badge";
export type { BadgeInput } from "./badge";
